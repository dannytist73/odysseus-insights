<!-- Country Selection Modal -->
<div
  id="countryModal"
  class="fixed inset-0 bg-black/80 backdrop-blur-sm flex items-center justify-center z-50"
>
  <div class="bg-white/10 backdrop-blur-md rounded-xl p-8 max-w-md w-full mx-4">
    <h2 class="text-[#7FFFD4] text-2xl font-semibold mb-6 text-center">
      Welcome to Voyage Compass
    </h2>
    <p class="text-white/80 mb-8 text-center">
      Please select your country to get started
    </p>

    <div class="space-y-4">
      <label class="block text-white mb-2">Your Country</label>
      <div class="relative">
        <select
          id="initialCountrySelect"
          class="w-full bg-white/5 border border-white/20 rounded-lg px-4 py-3 text-white focus:outline-none focus:ring-2 focus:ring-[#7FFFD4] [&>option]:text-gray-900"
        >
          <option value="">Select your country</option>
        </select>
        <div class="loading-spinner hidden"></div>
      </div>

      <button
        id="confirmCountry"
        class="w-full bg-[#7FFFD4] text-gray-900 text-lg font-semibold px-6 py-3 rounded-lg hover:bg-[#70E0C0] transition-all duration-300 mt-6 disabled:opacity-50 disabled:cursor-not-allowed"
        disabled
      >
        Continue
        <i class="fas fa-arrow-right ml-2"></i>
      </button>
    </div>
  </div>
</div>

<style>
  .loading-spinner {
    position: absolute;
    right: 12px;
    top: 50%;
    transform: translateY(-50%);
    width: 20px;
    height: 20px;
    border: 2px solid rgba(127, 255, 212, 0.3);
    border-radius: 50%;
    border-top-color: #7fffd4;
    animation: spin 1s linear infinite;
  }

  .loading-spinner.show {
    display: block;
  }

  @keyframes spin {
    to {
      transform: translateY(-50%) rotate(360deg);
    }
  }

  #countryModal {
    opacity: 0;
    transition: opacity 0.3s ease-in-out;
  }

  #countryModal.show {
    opacity: 1;
  }
</style>
